let depth = 0;

fun fibonacci(a) {
   if a <= 1 { return 1 }
   let val = fibonacci(a - 1) + fibonacci(a - 2);
   return val
}

fun test(b, a) {
    return a ** b + b ** a;
}

print test(1.2, 2.3);

print 'return value: ' + fibonacci(10)
